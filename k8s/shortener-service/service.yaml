apiVersion: v1
kind: Service
metadata:
  name: shortener-service           # this becomes the DNS name other Pods use
spec:
  selector:
    app: shortener-service          # routes traffic to Pods with this label
  ports:
    - port: 8001                    # port the Service exposes internally
      targetPort: 8001              # port the container actually listens on