apiVersion: v1
kind: Service
metadata:
  name: stats-service           # this becomes the DNS name other Pods use
spec:
  selector:
    app: stats-service          # routes traffic to Pods with this label
  ports:
    - port: 8002                    # port the Service exposes internally
      targetPort: 8002              # port the container actually listens on