name: Self-Hosted Runner Test

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: self-hosted

    steps:
      - name: Show runner information
        run: |
          echo "Runner is working!"
          hostname
          whoami

      - name: Check Kubernetes
        run: |
          kubectl get nodes
          kubectl get pods
